<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meeting Report</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @page { size: A4; margin: 2cm; }
    body { width: 210mm; height: 297mm; }
  </style>
</head>
<body class="bg-white font-sans text-gray-800">
  <header class="border-b-2 border-gray-300 pb-4 mb-6 flex justify-between items-center">
    <div>
      <h1 class="text-3xl font-bold text-blue-600">Meeting Report</h1>
      <p class="text-gray-700 text-lg mt-1">{{ organizationName }}</p>
    </div>
    <div class="text-right">
      <p class="text-gray-500 text-sm">Generated on: {{ new Date().toLocaleDateString() }}</p>
    </div>
  </header>
  <section class="mb-6">
    <h2 class="text-xl font-semibold text-gray-700 mb-2 border-b border-gray-300 pb-1">Meeting Details</h2>
    <table class="w-full text-gray-700 mt-2 table-fixed">
      <tbody>
        <tr>
          <td class="font-medium w-1/4 py-2">Title:</td>
          <td class="py-2">{{ meetingTitle }}</td>
        </tr>
        <tr class="bg-gray-50">
          <td class="font-medium py-2">Date:</td>
          <td class="py-2">{{ meetingDate }}</td>
        </tr>
        <tr>
          <td class="font-medium py-2">Time:</td>
          <td class="py-2">{{ meetingTime }}</td>
        </tr>
      </tbody>
    </table>
  </section>

  <section class="mb-6">
    <h2 class="text-xl font-semibold text-gray-700 mb-2 border-b border-gray-300 pb-1">Participants</h2>
    <ul class="list-disc list-inside text-gray-700 mt-2">
      @each(participant in participants)
        <li>{{ participant }}</li>
      @endeach
    </ul>
  </section>

  <section class="mb-6">
    <h2 class="text-xl font-semibold text-gray-700 mb-2 border-b border-gray-300 pb-1">Summary</h2>
    <p class="text-gray-700 leading-relaxed mt-2">{{ summary }}</p>
  </section>

  <footer class="mt-12 border-t border-gray-300 pt-4 text-gray-500 text-sm text-center">
    Zeekers Meeting App - Confidential
  </footer>

</body>
</html>
